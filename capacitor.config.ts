import type { CapacitorConfig } from '@capacitor/cli';

/**
 * ============================================================
 * إعدادات Capacitor - ASSASSIN FX
 * ============================================================
 * 
 * للتطوير (Lovable):
 * - قسم server يربط التطبيق بخادم Lovable للتحديث اللحظي
 * 
 * للإنتاج (بناء APK مستقل):
 * 1. احذف قسم server بالكامل
 * 2. غيّر appId إلى معرّف خاص بك (مثل: com.yourcompany.assassinfx)
 * 3. نفّذ: npm run build && npx cap sync android
 * 
 * للمزيد: راجع MIGRATION_GUIDE.md
 * ============================================================
 */

const config: CapacitorConfig = {
  // ============================================================
  // معرّف التطبيق - غيّره عند النشر على متجر التطبيقات
  // مثال للإنتاج: 'com.yourcompany.assassinfx'
  // ============================================================
  appId: 'app.lovable.ebc9336e82be4f7fb9ee83ee20c32755',
  
  // اسم التطبيق
  appName: 'ASSASSIN FX',
  
  // مجلد ملفات الويب المبنية
  webDir: 'dist',
  
  // ============================================================
  // إعدادات الخادم - للتطوير فقط!
  // ⚠️ احذف هذا القسم بالكامل عند بناء APK للإنتاج
  // ============================================================
  server: {
    // رابط خادم التطوير (Lovable Preview)
    url: 'https://ebc9336e-82be-4f7f-b9ee-83ee20c32755.lovableproject.com?forceHideBadge=true',
    cleartext: true
  }
  
  // ============================================================
  // إعدادات إضافية للإنتاج (اختياري)
  // ============================================================
  // plugins: {
  //   SplashScreen: {
  //     launchShowDuration: 2000,
  //     backgroundColor: '#000000',
  //   }
  // }
};

export default config;
